---
phase: 08-p0-manifest
type: execute
---

<objective>
Create .claude-plugin/plugin.json manifest and hooks.json for marketplace compatibility.

Purpose: Every top plugin ships these. Without them, we're invisible to marketplace discovery.
Output: .claude-plugin/plugin.json + .claude-plugin/hooks.json
</objective>
