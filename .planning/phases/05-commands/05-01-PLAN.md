---
phase: 05-commands
type: execute
---

<objective>
Create the 3 slash command entry points: /validate, /think, and /audit.

Purpose: Commands are the user-facing entry points that invoke skills. They're lightweight .md files with YAML frontmatter that route to the corresponding skill.
Output: 3 command .md files in .claude/commands/.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@CLAUDE.md
@.claude/skills/validate-approach/SKILL.md
@.claude/skills/think-cycle/SKILL.md
@.claude/skills/evidence-audit/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /validate command</name>
  <files>.claude/commands/validate.md</files>
  <action>
Create the /validate slash command that invokes the validate-approach skill.

The command file should:
1. Have YAML frontmatter with: name, description, and argument spec
2. Contain a clear prompt that:
   - Identifies the user's approach (from arguments or asks for it)
   - Invokes the validate-approach skill
   - Passes through all arguments (--frameworks, --framework-names)
3. Be concise — commands are thin wrappers around skills

Example invocations the command must handle:
- `/validate "Parse all XML files and classify by type"`
- `/validate @path/to/plan.md --frameworks 3`
- `/validate` (no args — should ask user for their approach)
  </action>
  <verify>cat .claude/commands/validate.md shows YAML frontmatter and skill invocation</verify>
  <done>Command file exists, routes to validate-approach skill, handles arguments</done>
</task>

<task type="auto">
  <name>Task 2: Create /think command</name>
  <files>.claude/commands/think.md</files>
  <action>
Create the /think slash command that invokes the think-cycle skill.

The command file should:
1. Have YAML frontmatter with: name, description, and argument spec
2. Contain a clear prompt that:
   - Identifies the subject to analyze (from arguments or asks for it)
   - Invokes the think-cycle skill
   - Passes through all arguments (--frameworks, --input-files, --expected-count)
3. Be concise — thin wrapper

Example invocations:
- `/think "Is our output correct?" --input-files data/output.json`
- `/think @data/cycle3_output.json --frameworks 3`
- `/think` (no args — should ask user what to analyze)
  </action>
  <verify>cat .claude/commands/think.md shows YAML frontmatter and skill invocation</verify>
  <done>Command file exists, routes to think-cycle skill, handles arguments</done>
</task>

<task type="auto">
  <name>Task 3: Create /audit command</name>
  <files>.claude/commands/audit.md</files>
  <action>
Create the /audit slash command that invokes the evidence-audit skill.

The command file should:
1. Have YAML frontmatter with: name, description, and argument spec
2. Contain a clear prompt that:
   - Identifies the target to audit (from arguments or asks for it)
   - Invokes the evidence-audit skill
   - Passes through all arguments (--strict, --source-dir)
3. Be concise — thin wrapper

Example invocations:
- `/audit data/output.json`
- `/audit @docs/report.md --strict`
- `/audit` (no args — should ask user what to audit)
  </action>
  <verify>cat .claude/commands/audit.md shows YAML frontmatter and skill invocation</verify>
  <done>Command file exists, routes to evidence-audit skill, handles arguments</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `ls .claude/commands/` shows validate.md, think.md, audit.md
- [ ] Each command has YAML frontmatter with name and description
- [ ] Each command routes to its corresponding skill
- [ ] Each command handles both "with arguments" and "no arguments" cases
</verification>

<success_criteria>
- 3 slash command files created
- All route to their respective skills
- All handle argument passing and no-argument prompting
- Plugin command layer is complete
</success_criteria>

<output>
After completion, create `.planning/phases/05-commands/05-01-SUMMARY.md`
</output>
