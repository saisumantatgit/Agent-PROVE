---
phase: 06-testing
type: execute
---

<objective>
End-to-end verification of the complete plugin, README documentation, and marketplace publish preparation.

Purpose: Verify all components work together, create user-facing documentation, and prepare the plugin for distribution.
Output: Verified plugin with README and marketplace-ready package.json.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
@~/.claude/skills/create-plans/references/checkpoints.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@CLAUDE.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Structural integrity verification</name>
  <files>All .claude/ files</files>
  <action>
Run comprehensive structural verification:

1. Verify all expected files exist:
   - 8 framework agents in .claude/agents/
   - 2 orchestration agents in .claude/agents/
   - 3 SKILL.md files in .claude/skills/*/
   - 3 command files in .claude/commands/
   - package.json in root
   - CLAUDE.md in root

2. Verify cross-references:
   - T1 validator references all 8 framework agent filenames correctly
   - Cycle auditor references framework agents correctly
   - Skills reference the correct orchestration agents
   - Commands reference the correct skills

3. Verify consistency:
   - All agent files follow Agent Communication Protocol (Role, inputs, questions, output, verdict, evidence)
   - All skills have YAML frontmatter (name, description)
   - All commands have YAML frontmatter (name, description)
   - No domain-specific references (ProSure, XBRL, DPM, BoE, EBA) in any file

4. Verify package.json:
   - Valid JSON
   - Name, version, description present
   - Keywords include "claude-code-plugin"

Report any failures with specific file paths and issues.
  </action>
  <verify>Run ls -R .claude/ to confirm file count; grep -r "ProSure\|XBRL\|DPM" .claude/ returns empty (no domain-specific refs)</verify>
  <done>All files exist, cross-references correct, no domain-specific leakage, package.json valid</done>
</task>

<task type="auto">
  <name>Task 2: Create README.md</name>
  <files>README.md</files>
  <action>
Create a comprehensive README.md with:

1. **Title and tagline** — "Thinking Framework Orchestrator" + one-liner
2. **What it does** — 3-bullet summary (T1 validation, cycle auditing, evidence protocol)
3. **Installation** — How to install as a Claude Code plugin (local path or marketplace)
4. **Quick Start** — Show the 3 commands with examples:
   - `/validate "your approach"` — pre-execution validation
   - `/think "your question" --input-files output.json` — per-cycle audit
   - `/audit data/output.json` — evidence compliance check
5. **Built-in Frameworks** — Table of all 8 with one-line descriptions
6. **Configuration** — How to customize:
   - `--frameworks N` to control how many
   - `--framework-names` to select specific ones
   - Custom frameworks: drop .md in .claude/agents/ following the template
7. **How It Works** — Brief architecture diagram (text-based):
   - User → Command → Skill → Orchestrator Agent → Framework Agents → Results
8. **Verdict Taxonomy** — What VALIDATED/REJECTED/PASS/FAIL/COMPLIANT mean
9. **Evidence Protocol** — What "zero uncited inference" means in practice
10. **Origin** — One paragraph crediting Mission 1086 methodology

Keep it under 200 lines. Practical, not academic.
  </action>
  <verify>cat README.md shows all 10 sections; wc -l README.md is under 200</verify>
  <done>README.md exists with all sections, under 200 lines, practical tone</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Thinking Framework Orchestrator plugin with 10 agents, 3 skills, 3 commands, package.json, CLAUDE.md, and README</what-built>
  <how-to-verify>
    1. Review: `ls -R .claude/` — confirm all 13 markdown files present
    2. Read: `cat README.md` — confirm documentation is clear and complete
    3. Read: `cat CLAUDE.md` — confirm conventions are accurate
    4. Spot-check: Read 2-3 agent files to verify quality and domain-agnosticism
    5. Confirm: Package ready for initial commit and local testing
  </how-to-verify>
  <resume-signal>Type "approved" to create final commit, or describe issues to fix</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Final commit and tag</name>
  <files>All files</files>
  <action>
Stage all files and create commit:
- Message: "Complete Thinking Framework Orchestrator v1.0 plugin"
- Tag: v1.0.0

This is the shippable v1.0 of the plugin.
  </action>
  <verify>git log --oneline shows commits; git tag shows v1.0.0; git status shows clean tree</verify>
  <done>All work committed, v1.0.0 tagged, working tree clean</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] All 16 files present (10 agents, 3 skills, 3 commands) + package.json + CLAUDE.md + README.md + .gitignore
- [ ] No domain-specific references anywhere in plugin
- [ ] README under 200 lines and covers all 10 sections
- [ ] package.json valid JSON
- [ ] Git tag v1.0.0 exists
- [ ] Working tree clean
</verification>

<success_criteria>
- Plugin structurally complete and verified
- README provides clear installation and usage guide
- No domain-specific leakage
- Tagged v1.0.0 and ready for distribution
- All BRIEF.md success criteria achievable with this plugin
</success_criteria>

<output>
After completion, create `.planning/phases/06-testing/06-01-SUMMARY.md`
</output>
